#!/bin/bash -e

cd ~jdtogni/dev/ib-gateway-docker/

case "$1" in
  start)
    echo -n "Starting DSTrader compose"
    dstrader/runtime/encr_files.sh decrypt
    docker-compose up
    dstrader/runtime/encr_files.sh clear
    echo "."
    ;;
  stop)
    echo -n "Stopping DSTrader compose"
    docker-compose down
    dstrader/runtime/encr_files.sh clear
    echo "."
    ;;
  restart)
    $0 stop
    $0 start
    ;;
  *)
  echo "Usage: /etc/init.d/dstrader {start|stop|restart}"
  exit 1
esac
exit 0
